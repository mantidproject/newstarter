#include <math.h>
#include <vector>
#include "Shapes.h"
using namespace std;

int main(int, char **)
{
	vector<Shape> shapes = { Square(2), Square(2.5), Rectangle(1, 2), Rectangle(2, 1), Triangle(2, 3), Triangle(3.1, 2.3), Circle(4.1) };

}