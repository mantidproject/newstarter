#include "ShapeSorter.h"
#include "Shape.h"
#include <vector>
#include <string>

int ShapeSorter::printByType(const std::vector<Shape> &shapes, const std::string &type) {
	for (const auto &shape : shapes) {

	}
	return 0;
}